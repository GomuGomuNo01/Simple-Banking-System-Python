# Copy this file to .env and adapt it to your MySQL server.
# Default values match a fresh WampServer installation (MySQL on port 3306, root without password).
SBS_DB_HOST=localhost
SBS_DB_PORT=3306
SBS_DB_USER=root
SBS_DB_PASSWORD=
SBS_DB_NAME=sbs_bank
